function getScores<T>(scores: T[]): T {
    const myIndex = 3;
    return scores[myIndex];
}

const searchScore = <T>(scores: T[]): T =>{
    const myIndex = 3
    return scores[myIndex]
}

export {};
